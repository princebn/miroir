apiVersion: v1
kind: Service
metadata:
  name: miroir-api
  labels:
    app: miroir-api
spec:
  type: ClusterIP
  selector:
    app: miroir-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
